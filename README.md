# Dice Roller

Whipped up a quick math helper for the boys. If you're a Docker-head and want to actually host the website on port 80, just run 'docker compose up' to build and launch the nginx server. If you're not, just open the index.html
